# Parking Escape Android

Android WebView wrapper for Parking Escape 1–150.

GitHub Actions builds a debug APK automatically after a push to `main`.
The APK is available in the workflow run under **Artifacts** as `parking_escape_apk`.
